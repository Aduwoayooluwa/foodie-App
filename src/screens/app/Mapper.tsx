import React from 'react'
import { View } from 'react-native'
type Props = {}

const Mapper = (props: Props) => {
    return (
        <View>
            {/* page for mapping. There will be a map here to check for location.
            here map api
            */}

            
        </View>
    )
}

export default Mapper